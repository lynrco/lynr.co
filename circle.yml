checkout:
  post:
    - git submodule sync
    - git submodule update --init
dependencies:
  bundler:
    without: [heroku]
test:
  post:
    - grunt heroku
    - mv dist/* $CIRCLE_ARTIFACTS
deployment:
  staging:
    branch: master
    heroku:
      appname: lynr-co-stage
