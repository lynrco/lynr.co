checkout:
  post:
    - git submodule sync
    - git submodule update --init
dependencies:
  bundler:
    without: [heroku]
deployment:
  staging:
    branch: master
    heroku:
      appname: lynr-co-stage
